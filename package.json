{
  "name": "settleguard",
  "version": "0.1.0",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "private": true,
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "SettleGuard — AI finance controller that reconciles payments, refunds, settlements, and bank credits, investigates mismatches with a bounded agent, and reports measured accuracy.",
  "scripts": {
    "proof": "tsx scripts/phase1-step1-proof.ts",
    "generate:tiny": "tsx scripts/generate-dataset.ts --preset tiny",
    "generate:demo": "tsx scripts/generate-dataset.ts --preset demo",
    "generate:benchmark": "tsx scripts/generate-dataset.ts --preset benchmark"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2"
  }
}
